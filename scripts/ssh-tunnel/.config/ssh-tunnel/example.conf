# SSH隧道示例配置文件
# 复制此文件并重命名为你的隧道名称.conf (如 web.conf, ssh.conf 等)

# 远程服务器信息
REMOTE_SERVER=your-server.example.com
# 动态IP解析服务器(可选, 留空则使用REMOTE\_SERVER)
# 通过 curl -s ${IP_SERVER} 获取动态IP
IP_SERVER=

REMOTE_USER=your_username
REMOTE_PORT=12345

# 本地服务信息
LOCAL_HOST=localhost
LOCAL_PORT=22
